<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- พื้นหลัง window ขาว (กัน splash/หน้าจอเริ่มต้นเป็นสีดำ) -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
