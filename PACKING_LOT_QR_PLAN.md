# เพิ่ม Flow Packing แบบสแกน QR LOT ก่อนสินค้า

## สรุป

ใช้วิธีสองขั้นบน Android Packing:

`สแกน QR LOT → เก็บ LOT รอจับคู่ → สแกนบาร์โค้ดสินค้า → ตรวจ SKU/LOT/EXP/สต็อก → บันทึกสินค้า`

ใช้ QR `LOT+EXP` ที่พิมพ์อยู่แล้ว ไม่เพิ่ม SKU และไม่ต้องพิมพ์ฉลากใหม่ ตัว parser รองรับทั้งข้อความสองบรรทัดและข้อความที่ Android ตัดบรรทัดออก จึงไม่ต้องออก APK ใหม่

## การเปลี่ยนแปลงหลัก

- เพิ่ม parser สำหรับ payload `LOT:{LOT}\nEXP:{EXP}` และรูปแบบ Android `LOT:{LOT}EXP:{EXP}` โดยไม่ตีความบาร์โค้ดสินค้าปกติเป็น QR LOT
- เพิ่มสถานะ `pendingLotQr` ในหน้า Packing พร้อมแถบแสดง LOT, EXP และสถานะรอสแกนบาร์โค้ดสินค้า
- QR หนึ่งครั้งใช้กับสินค้าถัดไปหนึ่งครั้งเท่านั้น; สแกน QR ใหม่ให้แทนค่าเดิม
- ตรวจว่า LOT อยู่ภายใต้ SKU, EXP ตรงกับ `lotMap` และยอด LOT คงเหลือเพียงพอกับ factor ของหน่วยที่สแกน
- คู่ที่ถูกต้องเข้าสู่ `applyScan` เดิมและบันทึก `scannedLots`, `scannedBarcode`, `scannedUnit`, LOT และ EXP ตามเดิม
- คู่ที่ไม่ตรง, ไม่พบสินค้า, สินค้าครบแล้ว หรือ LOT เหลือไม่พอ จะไม่เพิ่มจำนวนและล้าง QR ที่ค้าง
- กรณีสแกนเกินจำนวน เก็บ LOT ที่จับคู่ไว้ใน `confirmOver`; ยืนยันแล้วจึงบันทึก ส่วนยกเลิกให้ทิ้ง QR
- สินค้าที่ไม่มีข้อมูลใน `lotMap` ยังใช้ flow เดิม และสินค้าที่มี LOT แต่ไม่มี QR ใช้ทางสำรองเลือก/กรอก LOT เดิมได้
- ล้าง QR ที่ค้างเมื่อปิดลัง เปลี่ยนลัง เปลี่ยนพนักงาน หรือ Picklist เปลี่ยนรอบ
- Desktop Packing คงพฤติกรรมเดิม; QR-first ใช้เฉพาะ Android Packing

## อินเทอร์เฟซและความเข้ากันได้

- ไม่มี Firestore schema หรือ public API ใหม่
- เพิ่ม helper ภายในสำหรับ parse QR และตรวจการจับคู่กับ `lotMap`
- รองรับ `NOLOT`, `NOTLOT`, EXP ปี 9999 และฉลากที่พิมพ์ไว้แล้ว
- ไม่เพิ่มนโยบายบล็อกสินค้าหมดอายุ
- ไม่โหลด `qrProducts` ลง Android และไม่เปลี่ยน Android native scanner bridge

## การทดสอบ

- อ่าน QR ทั้งแบบมีบรรทัดใหม่และแบบ Android ตัดบรรทัด
- ป้องกันบาร์โค้ดสินค้าปกติถูกจำแนกเป็น QR
- ตรวจคู่ SKU/LOT/EXP และยอดคงเหลือตาม factor
- ตรวจ LOT ไม่อยู่ใน SKU, EXP ไม่ตรง, สินค้าไม่พบ/ครบแล้ว และ LOT ไม่พอ
- ตรวจ `NOLOT`, `NOTLOT` และ EXP ปี 9999
- ตรวจ over-scan, fallback เดิม และการล้างสถานะเมื่อเปลี่ยนขอบเขตงาน
- รันชุดทดสอบ Packing/LOT/Outbound เดิมและ production build

## สมมติฐาน

- QR ใช้ครั้งเดียวและล้างเมื่อสำเร็จหรือผิด
- ใช้ QR `LOT+EXP` เดิมเพื่อไม่ต้องพิมพ์ฉลากใหม่
- หากไม่มี QR มีทางสำรองแบบเดิมเพื่อไม่ให้งาน Packing หยุด
- ความถูกต้องของ SKU ตรวจหลังสแกนบาร์โค้ดสินค้า เพราะ QR เดิมไม่มี SKU
